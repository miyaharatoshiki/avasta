<h3>タグの追加</h3>

<!--
<%= render 'form' %>
-->

<%= form_for(@tag) do |f| %>
  <% if @tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag.errors.count, "error") %> prohibited this tag from being saved:</h2>

      <ul>
      <% @tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<br>
  <div class="field">
    <%= f.label :タグ名 %><br>
    <%= f.text_field :tag_name %>
  </div>
  <div class="actions">
    <%= f.submit :タグ追加 %>
  </div>
<% end %>


<br>
<br>
<%= link_to 'Back', '/pictures/new' %>
